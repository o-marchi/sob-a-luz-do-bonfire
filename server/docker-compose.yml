services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: sobaluz
    volumes:
      - pgdata:/var/lib/sobaluz/data
    ports:
      - '5432:5432'

volumes:
  pgdata:
